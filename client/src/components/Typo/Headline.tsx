import React, { PropsWithChildren } from 'react';

interface IHeadline extends PropsWithChildren {}

export const Headline: React.FC<IHeadline> = (props) => {
    const { children } = props;
    return <></>;
};
